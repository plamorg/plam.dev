---
const { href, name, id, url, github, image } = Astro.props;
---

<div class="w-full h-56 md:h-72 bg-center bg-cover bg-[#0008] bg-blend-darken" id={id} style={{backgroundImage: `url(${image});
`}}>
    <div class="w-full h-full flex flex-col justify-end text-white opacity-90 p-6 md:p-8">
        <a href={href}>
            <h2 class="font-serif text-xl">
                {name}
            </h2>
            <p class="mt-2 text-sm font-light">
                <slot />
            </p>
        </a>
    </div>
    
</div>
